# StudentManagement: Student Class Demonstration

This project implements a basic `Student` class to demonstrate object-oriented programming principles in C#.

## Features
- **Encapsulation:** Uses private fields with public properties.
- **Class Methods:**  
  - `Study()` — prints a message describing the student studying  
  - `Examination()` — prints the grade/class currently taking an exam  
  - `Tell()` — prints the student's age and name  
- **Main Program:**
  - Creates two student instances
  - Sets their properties
  - Calls class methods to display behavior

## Files
- `Student.cs` — class definition  
- `Program.cs` — main entry point  

## How to Compile
Simply open the folder in Visual Studio or another C# IDE and run `Program.cs`.


using System;

namespace Ex6_1
{
    public class Student
    {
        // Private fields
        private string name;
        private int id;
        private int age;
        private string grade;

        // Public properties for encapsulation
        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public int Id
        {
            get { return id; }
            set { id = value; }
        }

        public int Age
        {
            get { return age; }
            set { age = value; }
        }

        public string Grade
        {
            get { return grade; }
            set { grade = value; }
        }

        // Class methods
        public void Study()
        {
            Console.WriteLine("The student with ID {0} is studying", Id);
        }

        public void Examination()
        {
            Console.WriteLine("{0} is taking an examination", Grade);
        }

        public void Tell()
        {
            Console.WriteLine("Speaking now is a {0}-year-old student：{1}", Age, Name);
        }
    }
}


using System;

namespace Ex6_1
{
    class Program
    {
        static void Main(string[] args)
        {
            Student stu1 = new Student();
            stu1.Name = "Tom";
            stu1.Age = 20;
            stu1.Id = 2000001;
            stu1.Grade = "Materials Chemistry Class 22-1";

            stu1.Study();
            stu1.Examination();
            stu1.Tell();

            Console.WriteLine("----------------");

            Student stu2 = new Student();
            stu2.Name = "Mary";
            stu2.Age = 21;
            stu2.Id = 2000004;
            stu2.Grade = "Industrial Management Class 22-6";

            stu2.Study();
            stu2.Examination();
            stu2.Tell();

            // Output Mary’s student ID and age (as required by the assignment)
            Console.WriteLine("Mary's student ID is：{0}", stu2.Id);
            Console.WriteLine("Mary's age is：{0}", stu2.Age);

            Console.ReadKey();
        }
    }
}
